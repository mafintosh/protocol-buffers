{
  "name": "protocol-buffers",
  "version": "0.4.2",
  "description": "encode/decode protocol buffers in node with stream support",
  "repository": "mafintosh/protocol-buffers",
  "dependencies": {
    "varint": "^1.1.0"
  },
  "devDependencies": {
    "tape": "^2.12.3"
  },
  "scripts": {
    "test": "tape test/*.js"
  }
}
